let val;

// The documents can be used to grab different elements from the web page.

// val = document.html;
// val = document.links;
// val = document.scripts;

//  Using foreach to get scripts, which means turning it to an array before using the forEachach method.
// let scripts = document.scripts;

// let scriptArr = Array.from(scripts);

// scriptArr.forEach(function(script) {

//     console.log(script.getAttribute('src'));
// })


// console.log(val);